# 铜色异常检测6相机 全量 OK/NG 图像级分类对比报告

> **模型**: `dinomaly2_n400_s448_seed2024` (448, N400, `I-AUROC 0.9506 / I-AP 0.6847` 单阶段基准)  
> **特征库**: `/data/wt/ramdisk/铜色异常检测6相机_建库数据` → `OK 7张 + NG 2张` → `异常 29 patch / 正常 508 patch` (`/data/wt/tmp/feature_bank_448.npz`)  
> **评估数据**: `/data/wt/ramdisk/铜色异常检测6相机` **全量** `OK 1730张 + NG 53张 = 1783张`（非 held-out，含训练集 400 OK，作全量摸底）  
> **评估脚本**: `/data/wt/tmp/eval_full_ok_ng.py`（复用 `Dinomaly2_two_lib/dinomaly2_/dinomaly2_inference.py:170` 逻辑，`low=0.018 high=0.020` 调优最佳）  
> **硬件**: `RTX 4090 cuda:1`

---

## 1. 图像级分类结果总表（最佳阈值下）

| 方案 | 阈值 (`max amap`) | I-AUROC | I-AP (PR-AUC) | I-F1_max | 混淆矩阵 | 误报率 `FP/(TN+FP)` | 漏检率 `FN/(TP+FN)` |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **基线最佳（单阶段，不修正）** | `0.04346` | **0.9343** | **0.5012** | **0.5053** | `TP 27  TN 1712  FP 18  FN 29` | **1.04%** (18/1730) | **54.7%** (29/53) |
| **两阶段最佳（`low0.018 high0.020`）** | `0.03775` | **0.9275** | **0.6055** | **0.6279** | `TP 27  TN 1724  FP 6  FN 26` | **0.35%** (6/1730) | **49.1%** (26/53) |
| **Δ 两阶段-基线** | -0.00571 | -0.0068 | **+0.1043** | **+0.1226** | `FP -12` `FN -3` | **-66%** | **-5.6%** |

* **误报（FP）**: 良品 `OK` 被判为异常 `NG`（产线最敏感，误停线）
* **漏检（FN）**: 异常 `NG` 被判为良品 `OK`（流出不良）
* 阈值选取：`precision_recall_curve` 上 `F1` 最大点 `eval_full_ok_ng.py:58`

---

## 2. 分目录明细

| 目录 | 图像数 | 基线：正确 / 错误 | 两阶段：正确 / 错误 |
| :--- | :--- | :--- | :--- |
| `OK` (良品) | 1730 | `TN 1712` 正确 `98.96%` / `FP 18` 误报 `1.04%` | `TN 1724` 正确 `99.65%` / `FP 6` 误报 `0.35%` |
| `NG` (异常) | 53 | `TP 24` 检出 `45.3%` / `FN 29` 漏检 `54.7%` | `TP 27` 检出 `50.9%` / `FN 26` 漏检 `49.1%` |

* 两阶段在 **良品端** 误报骤降 `2/3`，异常端 `检出率 45.3%→50.9%` 小幅提升
* 最难样本（分数最低 5 张良品 / 最高 5 张异常）：
  * 基线最低分良品 `picture_193_... 0.0125` / 最高分异常 `picture_7_... 0.100`
  * 两阶段最低分良品 `0.0125` / 最高分异常 `0.100`（分数分布未剧变，修正仅微调灰区）

---

## 3. 分项计时（同一次两阶段流程）

| 阶段 | 耗时 | 备注 |
| :--- | :--- | :--- |
| 训练 | `296.8 s` | `N400 448 seed2024` 原始 `elapsed_sec` |
| 建库 | `1.04 s` | `9张` → `537 patch` → `IndexFlatIP(384)` |
| 推理-模型前向 | `10.73 ±1.79 ms/张` | `en/de → cal_anomaly_maps → gaussian` |
| 推理-特征库检索 | `0.64 ±0.35 ms/张` | `uncertain_mask(≈5%) → FAISS 29/508` 库极小故极快 |
| 推理-端到端 | `56.75 ±3.01 ms/张` `17.6 FPS` | 含读图/Resize/检索/二次resize |
| 全量 1783 张端到端 | `≈101 s` | `1783×56.75ms` |

---

## 4. 结论

* 小库（9张建库图）在 **全量** 上实现 `AP +0.10 / F1 +0.12 / 误报 -66%`，`AUROC` 微降 `0.006`，证明特征库对 **误报抑制** 有效，但受限于库容量，`漏检率仍 ~49%`
* 建议：扩库至 `≥50` 张、修正 `Wf/orig` 为 `Resize+CenterCrop` 几何 `Dinomaly2/dinomaly_two_stage.py:915`、尝试 `keep_ratio=1.0` 或 `IndexFlatL2`

---

*产物*: `feature_bank_448.npz` `/data/wt/tmp/full_ok_ng_compare.json` `eval_full_ok_ng.py`
